[default]
extend-ignore-re = [
    "[0-9a-fA-F]{6,}",
]

[default.extend-words]
crate = "crate"
AIMD = "AIMD"
overrideable = "overrideable"
# Sender retransmission timeout: standard SRT/TCP terminology, not a typo of "to".
rto = "rto"
