[package]
name = "pbt"
version = "0.0.0"
edition = "2024"
license = "Apache-2.0"
publish = false

[dev-dependencies]
proptest = "1.11"
srt_proto = { package = "srt-proto", path = "../", features = ["raw-codec"] }

[lints]
workspace = true
