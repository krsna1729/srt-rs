# Strict policy for the actually published dependency surface. This file has no
# advisory/yank exceptions; `deny.toml` separately audits the full benchmark
# workspace and documents its unpublished runtime exceptions.

[graph]
all-features = true
targets = [{ triple = "x86_64-unknown-linux-gnu" }]

[licenses]
version = 2
allow = ["MIT", "Apache-2.0", "Unicode-3.0"]
confidence-threshold = 0.93
